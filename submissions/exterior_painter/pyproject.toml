[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "exterior-painter-robot"
version = "0.1.0"
description = "MuJoCo tower painting descent robot demo"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "PyYAML>=6.0",
]

[project.optional-dependencies]
sim = [
  "imageio>=2.34.0",
  "imageio-ffmpeg>=0.5.0",
  "mujoco>=3.2.0",
]
dev = [
  "pytest>=8.0",
]

[project.scripts]
exterior-painter-robot = "exterior_painter_robot.__main__:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
exterior_painter_robot = ["assets/*.xml"]
